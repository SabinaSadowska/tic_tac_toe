export const selectSquares = (state) => state.game.squares;

export const selectTurn = (state) => state.game.turn;

export const selectScoreX = (state) => state.game.scoreX;

export const selectScoreO = (state) => state.game.scoreO;

export const selectGameStatus = (state) => state.game.gameStatus;
